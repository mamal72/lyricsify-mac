# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Lyricsify' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Lyricsify

  pod 'SwiftyJSON'
  
  pod 'Alamofire', '~> 4.0'
  
  pod 'Kanna', '~> 2.0.0' 

  target 'LyricsifyTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'LyricsifyUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
